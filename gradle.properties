kotlin.code.style=official

# Published coordinates — inherited by every subproject (replaces the old
# RootConventionPlugin `subprojects { group = ...; version = ... }`).
group=org.koaks.framework
version=0.0.1-snapshot1
